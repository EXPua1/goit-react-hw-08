import { Container, DocumentTitle, LoginForm, Section } from "../../components";

const LoginPage = () => {
  return (
    <Section>
      <Container>
        <div>
          <LoginForm />
        </div>
      </Container>
    </Section>
  );
};

export default LoginPage;
